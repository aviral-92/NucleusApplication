Please readme before continue:

1. First its Maven project, so import it as maven project to incluid all dependencies.

2. It will download all dependencies needed for the application to build.

3. Once it build successful:
	3.1. Go to application.properties and change mysql cedentials which is under src/main/resources.
	3.2. Import sql dump in mysql so that data is available.
	
4. Now run this application as SpringBoot app.

5. It will exposed to "localhost:6064/" .

6. In Manage tab, you will ask to insert username & password.

7. Default username & password is "aviral" & "mittal".

8. This application can be taken clone from GitHub and its url is "https://github.com/aviral-92/NucleusApplication" ;